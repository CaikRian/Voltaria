import Link from "next/link";

export function WebAnalyticsExport({ periodo, de, ate }: { periodo?:string; de?:string; ate?:string }) {
  const link = (formato:string) => { const params = new URLSearchParams({ formato }); if(periodo) params.set("periodo",periodo); if(de) params.set("de",de); if(ate) params.set("ate",ate); return `/api/painel/web-analise/exportar?${params}`; };
  return <section className="rounded-xl2 border border-indigo-200 bg-gradient-to-r from-indigo-50 via-white to-cyan-50 p-5 shadow-card"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-brand">Central de exportação</p><h2 className="mt-1 font-display text-lg font-bold">Leve toda a análise com você</h2><p className="mt-1 text-xs text-ink-muted">Resumo executivo, rankings, funil, origens e acessos detalhados do período selecionado.</p></div><div className="grid grid-cols-2 gap-2 sm:flex">{[["pdf","PDF","Documento visual"],["xlsx","Excel","Planilha editável"],["csv","CSV","Dados tabulares"],["json","JSON","Dados completos"]].map(([format,label,note])=><Link key={format} href={link(format)} className="group rounded-xl border border-line bg-white px-4 py-2.5 shadow-sm transition hover:-translate-y-0.5 hover:border-brand hover:shadow-card"><strong className="block text-xs group-hover:text-brand">↓ {label}</strong><span className="text-[9px] text-ink-muted">{note}</span></Link>)}</div></div></section>;
}
